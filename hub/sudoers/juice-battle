# Juice Battle — passwordless service + BlueZ control on the kiosk, so deploy.sh,
# the udev hot-plug rules, and the BLE scanner never hit a password prompt.
# Installed to /etc/sudoers.d/juice-battle by setup.sh (mode 0440, visudo-checked).
# Byte-for-byte the file field-proven on AQ3.
arduino ALL=(root) NOPASSWD: /usr/bin/hciconfig, /usr/bin/systemctl
